#include "main.h"
#include <stdio.h>
/**
* infinite_add - Adds two numbers
* @n1: first input string
* @n2: second input string
* @r: pointer to stored results
* @size_r: requested buffer size
* Return: pointer to stored results in buffer
*/
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
int i, j, n;
i = j = n = 0;
return (r);
}
