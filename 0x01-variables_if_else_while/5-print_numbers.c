#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* Prints single digit of base 10 starting at 0 followed by new line
* Return: (0)
*/

int main(void)
{
int sindig;
while (sindig < 10)
{
printf("%d", singdig);
singdig += 1;
}
printf("\n");
return (0);
}
