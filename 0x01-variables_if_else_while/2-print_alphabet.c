#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints alphabet in lowercase
 * putchar use only
 * Return: 0
 */

int main(void)
{
int A = 'a';
while (A <= 'z')
{
putchar(A);
l += A;
}
putchar('\n');
return (0);
}
