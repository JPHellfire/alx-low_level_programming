#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints alphabet in lowercase
 * putchar use only
 * Return: 0
 */

int main(void)
{
int a = 'a';
while (a <= 'z')
{
putchar(a);
l += 1;
}
putchar('\n');
return (0);
}
